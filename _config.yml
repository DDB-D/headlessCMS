# this file is *NOT* reloaded automatically when use 'bundle exec jekyll serve'.
# If change this file, restart server process.

title: HARTGELD · FORM · CODE · TYPE
email: mail@holgerardelt.de
description: >
  concept art-direction motion ux ui prototype development
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.holgerardelt.de/" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: hlgrrdlt
github_username:  hartgeld

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
sass:
    # style: compact
    load_paths:
      - _sass
      - node_modules/normalize-scss/sass
exclude: [vendor]
assets:
   sources:
     -assets/fonts
     -assets/javascript
     -assets/graphics
     -assets/videos
     -node_modules
